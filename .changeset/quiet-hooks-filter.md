---
"rolldown-plugin-react-forward-ref": patch
---

Add a transform hook filter so Rolldown and Vite skip handler calls for unsupported files, declarations, dependencies, virtual modules, and raw or URL requests.
